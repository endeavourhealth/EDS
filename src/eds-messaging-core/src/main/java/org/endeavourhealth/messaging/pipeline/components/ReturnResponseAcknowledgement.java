package org.endeavourhealth.messaging.pipeline.components;

import org.endeavourhealth.messaging.exchange.Exchange;
import org.endeavourhealth.messaging.pipeline.PipelineProcess;

public class ReturnResponseAcknowledgement implements PipelineProcess {
	@Override
	public void process(Exchange exchange) {
		// Figure out the transformer
		// Call it
		// Replace message body in echange
	}
}
