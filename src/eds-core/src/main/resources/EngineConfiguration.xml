<engineConfiguration>

  <!-- Cassandra settings -->
  <cassandra>
    <!--<username>string</username>-->
    <!--<password>string</password>-->
    <node>127.0.0.1</node>
  </cassandra>

  <!-- non-logback logging settings (see logback.xml for others) -->
  <!--<logging>
    <keyspace>logging</keyspace>
  </logging>-->

  <!-- audit settings -->
  <!--<audit>
    <keyspace>audit</keyspace>
  </audit>-->

</engineConfiguration>